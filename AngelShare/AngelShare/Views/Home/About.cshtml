@model IEnumerable<AngelShare.Models.Survey>

@{
    ViewBag.Title = "About";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CoffeeQuestionResponse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NoseQuestionResponse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WhiskeyDrinkResponse)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CoffeeQuestionResponse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NoseQuestionResponse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WhiskeyDrinkResponse)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.SurveyId }) |
                @Html.ActionLink("Details", "Details", new { id = item.SurveyId}) |
                @Html.ActionLink("Delete", "Delete", new { id = item.SurveyId })
            </td>
        </tr>
    }

</table>